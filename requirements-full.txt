# Core Dependencies for Lung ASP
nibabel>=5.0.0
numpy>=1.24.0
scipy>=1.10.0
scikit-image>=0.20.0
trimesh>=3.20.0
matplotlib>=3.7.0
pillow>=9.5.0
pydicom>=2.3.0
SimpleITK>=2.2.0
totalsegmentator>=2.0.0

# DICOM to NIfTI conversion
dicom2nifti>=2.4.0

# nnU-Net for advanced segmentation
nnunetv2>=2.2

# PyInstaller for creating executables
pyinstaller>=6.0.0

# Additional dependencies for GUI
tk>=0.1.0  # Usually included with Python, but listed for completeness

# PyTorch with CUDA support (for GPU acceleration)
# Note: Install separately with: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118

